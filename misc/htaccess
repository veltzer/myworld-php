# BEGIN WordPress
<IfModule mod_rewrite.c>
RewriteEngine On
#RewriteBase /
#RewriteCond %{REQUEST_FILENAME} !-f
#RewriteCond %{REQUEST_FILENAME} !-d
#RewriteCond %{REQUEST_FILENAME} !-f
#RewriteRule . /blog/index.php [L]
# see http://www.yourhtmlsource.com/sitemanagement/urlrewriting.html for working with
# mod_rewrite
# anyone who does not supply a url (just the name of the site) should get the blog
RewriteRule ^$ /blog/index.php 
</IfModule>
# END WordPress

# This is for being able to upload big files to php
php_value upload_max_filesize 20M
php_value post_max_size 20M
php_value max_execution_time 200
php_value max_input_time 200
